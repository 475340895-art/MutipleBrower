<hc:Window x:Class="FingerprintBrowser.Views.ProxyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="代理管理"
        Height="650" Width="900"
        MinHeight="500" MinWidth="800"
        WindowStartupLocation="CenterOwner"
        Background="#0f0f1a"
        NonClientAreaBackground="#12121f"
        NonClientAreaForeground="White">

    <Window.Resources>
        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00d4ff" Offset="0"/>
            <GradientStop Color="#7c3aed" Offset="1"/>
        </LinearGradientBrush>
        
        <Style x:Key="TechButton" TargetType="Button">
            <Setter Property="Background" Value="#2d2d44"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3d3d5c"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource TechButton}">
            <Setter Property="Background" Value="{StaticResource AccentGradient}"/>
        </Style>
        
        <Style x:Key="TechTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1a1a2e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3d3d5c"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#667eea"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="TechComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#1a1a2e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3d3d5c"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="代理管理" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

        <!-- 工具栏 -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,16">
            <Button Style="{StaticResource PrimaryButton}" Content="+ 添加代理" Click="AddProxy_Click" Margin="0,0,12,8"/>
            <Button Style="{StaticResource TechButton}" Content="批量导入" Click="BatchImport_Click" Margin="0,0,12,8"/>
            <Button Style="{StaticResource TechButton}" Content="批量测试" Click="BatchTest_Click" Margin="0,0,12,8"/>
            <Button Style="{StaticResource TechButton}" Content="删除选中" Click="DeleteSelected_Click" Margin="0,0,12,8"/>
            <Separator Style="{StaticResource VerticalSeparatorStyle}" Margin="8,0"/>
            <TextBox x:Name="SearchBox" Width="200" Style="{StaticResource TechTextBox}" 
                     hc:InfoElement.Placeholder="搜索代理..." Margin="0,0,12,8"
                     TextChanged="SearchBox_TextChanged"/>
        </WrapPanel>

        <!-- 代理列表 -->
        <DataGrid Grid.Row="2" x:Name="ProxyGrid" AutoGenerateColumns="False"
                  Background="Transparent" BorderThickness="0" GridLinesVisibility="None"
                  HeadersVisibility="Column" RowBackground="Transparent"
                  AlternatingRowBackground="#151525" CanUserAddRows="False"
                  SelectionMode="Extended" CanUserResizeRows="False">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#0f0f1a"/>
                    <Setter Property="Foreground" Value="#8888aa"/>
                    <Setter Property="Padding" Value="12,10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#e0e0e0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#2a2a4a"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1e1e32"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Width="40"/>
                <DataGridTemplateColumn Header="状态" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Ellipse Width="10" Height="10" HorizontalAlignment="Center">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#666688"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Available">
                                                <Setter Property="Fill" Value="#00ff88"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Unavailable">
                                                <Setter Property="Fill" Value="#ff4466"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Testing">
                                                <Setter Property="Fill" Value="#ffaa00"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="协议" Binding="{Binding Type}" Width="70"/>
                <DataGridTextColumn Header="地址" Binding="{Binding Host}" Width="180"/>
                <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="80"/>
                <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="120"/>
                <DataGridTextColumn Header="延迟" Binding="{Binding Latency, StringFormat='{}{0}ms'}" Width="80"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
                <DataGridTemplateColumn Header="操作" Width="140">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="测试" Style="{StaticResource TechButton}" 
                                        Padding="8,4" Margin="0,0,4,0" Click="TestProxy_Click"/>
                                <Button Content="删除" Style="{StaticResource TechButton}" 
                                        Padding="8,4" Foreground="#ff6688" Click="DeleteProxy_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 底部状态 -->
        <Border Grid.Row="3" Background="#12121f" CornerRadius="8" Padding="16,12" Margin="0,16,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="ProxyCountText" Text="共 0 个代理" Foreground="#8888aa" VerticalAlignment="Center"/>
                <TextBlock Text=" | " Foreground="#444466" Margin="12,0"/>
                <Ellipse Width="8" Height="8" Fill="#00ff88" Margin="0,0,6,0"/>
                <TextBlock x:Name="AvailableCountText" Text="可用: 0" Foreground="#8888aa" VerticalAlignment="Center"/>
                <TextBlock Text=" | " Foreground="#444466" Margin="12,0"/>
                <Ellipse Width="8" Height="8" Fill="#ff4466" Margin="0,0,6,0"/>
                <TextBlock x:Name="UnavailableCountText" Text="不可用: 0" Foreground="#8888aa" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</hc:Window>
